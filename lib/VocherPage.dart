import 'package:flutter/material.dart';

class VocherPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Vocher'),
      ),
      body: Center(
        child: Text('This is the Home Page'),
      ),
    );
  }
}
